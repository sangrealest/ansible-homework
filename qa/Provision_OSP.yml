---
- hosts: jumpbox
  roles:
     - role: osp-network
     - role: osp-keypair
     - role: osp-security
     - role: osp-flavor
     - role: osp-servers
     - role: base-config
       tags: base-config


     - role: lb-tier
       tags: 
         - lbs
         - haproxy
 
 # Setup application servers tier
 
     - role: base-config
       tags: base-config
     - role: app-tier
       tags: 
         - apps
         - tomcat
 
 # Setup database tier
 
     - role: base-config
       tags: base-config
     - role: geerlingguy.postgresql
       tags: 
         - dbs
         - postgres
 
